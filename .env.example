# No server-side env vars needed — users supply their own SF credentials via the UI.
# If you want to pre-fill defaults for development, you can add them here:

# NEXT_PUBLIC_DEFAULT_SF_DOMAIN=mycompany.my.salesforce.com
